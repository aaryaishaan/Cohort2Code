// 1 WAP that prints all the even numbers in an array

let employ_arr= [`ishaan`, `indra`, `sahil`,` arnav`, `arunav`, `anurag`, `gopal`];

let employ_age=[ 20,19,24,18, 23,40, 33];
// console.log(employ_arr.length);
// console.log(employ_age.length);

for(let i= 0; i<employ_age.length; i++){

  if( employ_age[i]%2 == 0) console.log(employ_age[i]);
//  else continue;

}